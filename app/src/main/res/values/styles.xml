<resources>

    <!-- Base application theme. -->
    <style name="Theme.Inspiee" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

    </style>

    <!--<style name="Theme.Inspiee.windowAnimations">
        <item name="@android:activityOpenExitAnimation">@anim/standstill</item>
        <item name="@android:activityOpenEnterAnimation">@anim/right_slide_out</item>
        <item name="@android:activityCloseEnterAnimation">@anim/standstill</item>
        &lt;!&ndash;<item name="@android:activityCloseExitAnimation">@anim/right_slide_in</item>&ndash;&gt;
    </style>-->

</resources>
